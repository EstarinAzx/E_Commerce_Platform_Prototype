# Frontend Environment Variables (for Vercel)
# Copy this to .env.local for local development

# Backend API URL (set this to your Railway backend URL after deploying)
VITE_API_URL=http://localhost:3000
